# ┌───────────── Python bytecode ─────────────┐
__pycache__/
*.py[cod]
*$py.class
# └───────────────────────────────────────────┘

# ┌────────────── Virtual environments ───────────────┐
.env/
.venv/
env/
venv/
ENV/
env.bak/
venv.bak/
# └───────────────────────────────────────────────────┘

# ┌─────────────── IDE / Editor folders ───────────────┐
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
# └────────────────────────────────────────────────────┘

# ┌────────────── Jupyter / Notebook ──────────────┐
.ipynb_checkpoints
# └────────────────────────────────────────────────┘

# ┌────────────── Data files & exports ──────────────┐
data/raw/
data/processed/
data/interim/
data/external/
# └──────────────────────────────────────────────────┘

# ┌──────── Model artifacts & MLflow logs ────────┐
models/
models/*.pkl
models/*.joblib
mlruns/
# └───────────────────────────────────────────────┘

# ┌─────────────── Docker & Compose ────────────────┐
Dockerfile*
docker-compose*.yml
# └──────────────────────────────────────────────────┘

# ┌─────────────── Logs & reports ────────────────┐
logs/
*.log
# └─────────────────────────────────────────────────┘

# ┌────────────── Packaging & builds ──────────────┐
build/
dist/
*.egg-info/
# └─────────────────────────────────────────────────┘

# ┌─────────── Test / Coverage caches ────────────┐
.pytest_cache/
coverage.xml
htmlcov/
# └─────────────────────────────────────────────────┘
